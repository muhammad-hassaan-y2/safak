import React from 'react'

const BlogLayout = ({
    children,
  }: {
    children: React.ReactNode
  }) => {
  return (
    <div>
        This is a blog layout
    
    </div>
  )
}

export default BlogLayout